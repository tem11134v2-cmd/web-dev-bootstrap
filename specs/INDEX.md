# specs/ — последовательность задач

Спеки — единицы работы для Claude Code. **Одна спека = одна сессия = один коммит-набор** — или одна оркестраторная волна (см. `docs/orchestration.md`). Запускаются командой:

```
Read and execute specs/04-homepage-and-approval.md.
Before coding:
1. Read the KB files listed in the spec.
2. Show me your implementation plan.
3. Wait for my approval before writing any code.
```

После завершения спеки — `/clear`, открытие следующей. Прыгать через спеки нежелательно: в каждой явно или неявно зашиты артефакты предыдущей.

## Основной поток (00 → 14)

> **Перед запуском любой спеки** разработчик уже прошёл `_BUILD/HOW-TO-START.md` §0-§3: установил mise + gh + git identity, создал папку и репо через `gh repo create --template`, открыл проект в Claude Desktop. Это *не спека*, а ручной ритуал — описан в HOW-TO, не дублируется в `specs/`.

| № | Спека | Цель | Зависит от | Деплой после |
|---|---|---|---|---|
| 00 | `00-brief.md` | Принять материалы заказчика, разложить в `docs/spec.md`, `docs/content.md`, `docs/pages.md`, `docs/integrations.md` | HOW-TO §0-§3 | нет |
| 01a | `01a-local-setup.md` | Проверить тулчейн на Mac (node, pnpm, git, gh), git-идентичность, SSH к GitHub, `.tool-versions`, зафиксировать в memory | 00 | нет |
| 01b | `01b-server-handoff.md` | Сгенерировать в репо: `.github/workflows/deploy-*.yml` (push-based по шаблонам из `_BUILD/templates/`), `deploy/{site}.caddy.example`, `deploy/README.md` (из `_BUILD/templates/deploy-readme.md.example` — чек-лист: SSH-ключ, GitHub Environment Secrets + variables `PROD_PORT`/`DEV_PORT`, mkdir releases/). VPS поднимается по `docs/server-manual-setup.md` + `docs/server-add-site.md` (SSH — по канону CLAUDE.md). **Гейт:** зелёный Actions-run + `https://{domain}` отвечает 200 — до 04 не переходим | 01a | сервер готов принять билд |
| 02 | `02-project-init.md` | `create-next-app`, структура папок, базовые зависимости, скрипты, scaffold | 01a (01b параллельно — пока пользователь настраивает сервер) | первый билд (пустой) |
| 03 | `03-design-system.md` | Цвета и шрифты из `docs/spec.md` в `globals.css`, базовый Header/Footer | 02 | дизайн-каркас на dev |
| 04 | `04-homepage-and-approval.md` | Главная по `docs/content.md` + промежуточный деплой + одобрение заказчика | 03 | главная на dev/prod |
| 05 | `05-subpages-template.md` | `ServicePageTemplate` + 1 пилотная страница услуги | 04 | первая услуга на dev |
| 06 | `06-subpages-rollout.md` | Все остальные страницы услуг по `docs/pages.md` (HIGH priority) | 05 | все услуги на dev/prod |
| 07 | `07-blog-optional.md` | MDX-блог с listing/post/sitemap (если в `docs/pages.md` есть блог) | 02 (минимум), 06 (рекомендуется) | блог на dev |
| 08 | `08-seo-schema.md` | robots.ts, sitemap.ts, Schema.org, редиректы, проверка в валидаторах | 06 | весь сайт SEO-готов |
| 09 | `09-forms-crm.md` | Server Action `submitLead`, минимальный трек «email-первый» (почта сегодня, остальные sinks — когда попросят), fallback в `LEADS_DIR` (`data/leads.jsonl`), ConsultationDialog, cookie-баннер, страницы `/privacy` + `/consent` | 06 | формы рабочие |
| 10 | `10-analytics.md` | Я.Метрика + GA + GSC + Я.Вебмастер + цели | 09 (для конверсий) | счётчики собирают |
| 11 | `11-performance.md` | Bundle analyzer, Server Components, dynamic imports, изображения, `loading.tsx` ревизия, Lighthouse 90+ | все основные | оптимизированный prod |
| 12 | `12-handoff.md` | Передача доступов, `HANDOFF.md` с runbook, тег v1.0 | 11 | релизная версия |
| 13 | `13-extend-site.md` | **Циклическая** спека для post-launch расширений (новая страница, блок, статья, точечная правка) | 12 | по запросу |
| 14 | `14-migrate.md` | Переезд сайта между VPS (M1–M4: более мощный сервер / handoff / emergency / клон на новый домен). Scp runtime-данных, DNS switch, 7-day soak, decommission | по запросу (обычно после 12 или между 10 и 11 при масштабировании) | новый VPS |

**Граф зависимостей (текстом):**

```
[HOW-TO §0-§3 — установка Mac, gh repo create, открыть в Claude]
       ↓
       00 → 01a ─┬─→ 02 → {дизайн} → 05 → 06 ─┬─→ 08 ─→ 11 → 12 → 13 (loop) → 14 (по запросу)
                 │                            ├─→ 09 ─→ 10 ─→ 11
                 │                            └─→ 07 (опционально, после 02)
                 └─→ 01b (параллельно 02–06, пока ты настраиваешь VPS)

{дизайн} — развилка «Источник дизайна», выбранная в 00:
   C  с нуля по брифу:  03 → 04
   B  референсы:        opt-design-from-references → 03 → 04
   A  готовый макет:    opt-design-to-site (заменяет 03, упрощает 04 — одобрение заказчика остаётся)
   D  донор 1:1:        opt-visual-recreate (заменяет 03–06 → сразу к 08)
```

`01b` блокирует только **первый deploy** — Claude и пользователь могут делать 02–06 локально, пока VPS готовится. К моменту 04 (одобрение заказчика) сервер уже должен принимать push.

## Опциональные спеки (`specs/optional/`)

Применяются по требованию проекта, не входят в основной поток.

| Спека | Когда применять | Где встроить |
|---|---|---|
| `opt-quiz.md` | Нужен квиз/калькулятор как лид-магнит | После 09 (общий `app/actions/submit-lead.ts`) |
| `opt-ecommerce.md` | Сайт продаёт физические товары (до ~200 SKU без полноценной БД) | Заменяет 05–06 |
| `opt-i18n.md` | Сайт на 2+ языка (next-intl) | После 03, до или вместе с 04 |
| `opt-design-to-site.md` | Ветка A из 00: есть готовый макет (Claude Design / HTML / скрины) | **Заменяет 03, упрощает 04** (одобрение заказчика остаётся); после 02 |
| `opt-design-from-references.md` | Ветка B из 00: макета нет, есть 2–5 URL референсов/конкурентов | Перед / вместо первой половины 03; дальше обычные 03/04 |
| `opt-migrate-from-existing.md` | Есть действующий сайт (Tilda, WP, ...), нужно сохранить SEO + контент | **До или параллельно 00** |
| `opt-visual-recreate.md` | Ветка D из 00 («перенос-реконструкция»): дизайн донора 1:1 на наш стек (визуально, без выгрузки кода) | **Заменяет 03–06**; после/вместе с opt-migrate |

## Шаблоны (`specs/templates/`)

| Файл | Назначение |
|---|---|
| `spec-template.md` | Базовый шаблон любой спеки. Копируй и заполняй |
| `page-spec-template.md` | Шаблон спеки для одной страницы. Используется в 05, 06, 13 |
| `client-todo-template.md` | Шаблон `CLIENT-TODO.md` (корень проекта) — «что нужно от заказчика». Создаётся в 00, пополняется по триггеру из CLAUDE.md; спеки 01b/04/09/10/12 пишут в него явно |

## Примеры (`specs/examples/`)

**Не задачи к выполнению** — образцы зрелых спек из реального проекта. Используй как референс формата (объём, уровень детализации, баланс «требования / задачи / критерии»). Примеры из pre-v3-эпохи: формат смотри, команды и стек НЕ копируй — см. плашку в `specs/examples/README.md`.

| Файл | Чему учит |
|---|---|
| `example-20-server-components-refactor.md` | Декомпозиция крупного архитектурного рефакторинга, описание critical complexity |
| `example-25-performance-audit.md` | Финальный performance pass, ссылки на Background «already done», конкретные команды |

## Правила работы со спеками

- **Plan mode** обязателен в начале каждой спеки — `Shift+Tab×2`. Спека = требования, план — путь реализации.
- **Строка `> Оркестрация:` в шапке каждой спеки** — кто её исполняет (builder-sonnet / builder-opus / fan-out), с чем можно параллелить, нужен ли независимый verifier. Действует в оркестраторном режиме (`docs/orchestration.md`); в интерактиве — можно игнорировать.
- **«KB files to read first»** — Claude грузит **только** перечисленные файлы из `docs/`. Не «всё подряд».
- **Один коммит на подзадачу** (см. секцию `Tasks` в каждой спеке). Это save points.
- **Memory updates** в конце каждой спеки — обновить `.claude/memory/project_state.md` (отметить done, указать следующую) и другие файлы по триггерам из CLAUDE.md.
- **Если спека не помещается в одну сессию** (контекст > 60%) — `/clear`, написать handoff.md, продолжить в новой сессии.
- **Никогда не правь спеки задним числом** «как было сделано». Если факт universal — он уходит в `docs/`. Если specific — в `.claude/memory/`. Спека отражает план, не журнал.

## Расширение

Когда возникает задача за пределами основного потока (00 → 14):

- **Новая страница, блок или статья** — `13-extend-site.md` (циклическая).
- **Полный редизайн** — новая спека `03-design-system v2`.
- **Новая большая фича** (личный кабинет, мультиязычность позже) — отдельная спека или `optional/`.
- **Полный переаудит SEO/perf** — повтор `08-seo-schema` или `11-performance` целиком.
- **Переезд на другой VPS** — спека `14-migrate`.

Не путай «расширение» с «новой большой задачей» — для второго пиши отдельную спеку с планом и оценкой, а не размазывай в `13-extend-site`.
