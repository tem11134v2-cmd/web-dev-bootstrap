---
name: lessons
description: Конкретные кейсы — что сломалось в этом проекте и как починили
type: project
---

# Уроки проекта

Записывай инциденты и фиксы, специфичные для этого сайта.
Универсальные уроки (применимые к любому проекту) — в общую `docs/performance.md`
или в user-memory разработчика.

## Шаблон записи

```markdown
### [YYYY-MM-DD] [Краткое описание проблемы]

**Симптом:** [что наблюдали — ошибка, метрика, поведение]

**Причина:** [корневая причина после диагностики]

**Фикс:** [что конкретно сделали — файл:строка или команда]

**Профилактика:** [как избежать повторения — линт-правило, тест, чек-лист]
```

## Записи

<!-- Примеры (удалить при заполнении реальными):

### [2026-03-15] PSI mobile упал с 92 до 67 после добавления квиза

**Симптом:** После релиза квиза в Hero на главной — Lighthouse mobile упал на 25
баллов. Desktop не пострадал.

**Причина:** QuizWidget — большой client-component с State, импортирован
синхронно в server-компонент Hero. Гидратация блокировала LCP.

**Фикс:** `app/page.tsx` — заменить на `dynamic(() => import('@/components/QuizWidget'),
{ loading: () => <div className="h-96 animate-pulse" /> })`.

**Профилактика:** Все client-компоненты ниже первого экрана грузить через
`next/dynamic`. Добавлено в чек-лист `specs/11-performance.md`.

---

### [2026-03-22] Формы не доходили до Bitrix — 401 на их вебхуке

**Симптом:** Формы отправлялись успешно (toast «Спасибо!»), но в CRM лиды
не появлялись. Логи: 401 Unauthorized.

**Причина:** Bitrix отозвал старый webhook-токен после смены пароля админа.

**Фикс:** Сгенерирован новый webhook в Bitrix → обновлён `BITRIX_WEBHOOK_URL`
в `.env` на проде → `pm2 restart`.

**Профилактика:** Добавлен fallback в `data/leads.json` (был, но не проверялся).
Добавлен мониторинг: cron каждые 6 часов проверяет последний лид в CRM.
-->

[Заполняется по ходу проекта]
